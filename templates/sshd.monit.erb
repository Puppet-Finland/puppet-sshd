### THIS FILE IS MANAGED BY PUPPET. ANY MANUAL CHANGES WILL GET OVERWRITTEN.

check process sshd with pidfile /var/run/sshd.pid
   start program = "<%= scope.lookupvar('sshd::params::service_command') %> start"
   stop  program = "<%= scope.lookupvar('sshd::params::service_command') %> stop"
   alert <%= scope.lookupvar('sshd::monit::monitor_email') %> with reminder on 480 cycles
